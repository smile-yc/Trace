# Trace Design System

Trace 是面向个人与小团队的工作记录、成果沉淀和周期复盘工具。界面应像可靠的工作台：安静、清晰、可扫描，让数据和行动优先于装饰。

本文件是项目后续 UI 修改的唯一视觉规范。新增页面和组件必须复用这里的设计令牌与组件规则，不得另起一套颜色、圆角、阴影或间距语言。

## Design Principles

1. **Content first**：先呈现判断和行动，再呈现辅助解释。
2. **Quiet surfaces**：以白色表面、冷灰画布和细边框组织内容，不使用装饰性大色块。
3. **One accent**：品牌蓝只用于主操作、选中态、焦点和关键数据；状态色只表达状态。
4. **Compact clarity**：企业工具需要高扫描效率。控件紧凑，但不压缩可点击区域和文字行高。
5. **Trace line**：选中导航、章节标题和关键指标可使用 2-3px 的蓝色轨迹线，作为系统唯一识别元素。

## Design Tokens

### Color

| Role | Token | Value |
| --- | --- | --- |
| Page canvas | `--color-page` | `#F7F8FA` |
| Primary surface | `--color-surface` | `#FFFFFF` |
| Muted surface | `--color-surface-muted` | `#F2F4F7` |
| Title | `--color-title` | `#101828` |
| Body text | `--color-text` | `#344054` |
| Muted text | `--color-text-muted` | `#667085` |
| Border | `--color-border` | `#E4E7EC` |
| Brand | `--color-brand` | `#2563EB` |
| Brand selected | `--color-brand-selected` | `#EFF6FF` |
| Success | `--color-success` | `#027A48` |
| Warning | `--color-warning` | `#B54708` |
| Danger | `--color-danger` | `#B42318` |

禁止蓝紫渐变、彩色阴影和仅用于装饰的状态色。

### Radius And Shadow

- 控件圆角：`6px`
- 卡片、面板、弹窗圆角：`8px`
- 标签圆角：`999px`，仅用于短状态文本
- 常规卡片只使用边框，不使用阴影
- 浮层只使用 `0 16px 40px rgba(16, 24, 40, 0.14)`

### Spacing

使用 `4 / 8 / 12 / 16 / 20 / 24 / 32px` 的间距阶梯。

- 页面横向留白：桌面 `32px`，平板 `24px`，手机 `16px`
- 页面模块间距：`16px`
- 卡片内边距：默认 `20px`，紧凑卡片 `16px`
- 表单字段间距：`16px`
- 标题与正文间距：`4-8px`

## Typography

字体栈：`Inter, Segoe UI, PingFang SC, Microsoft YaHei, system-ui, sans-serif`。

| Role | Size / Line height | Weight |
| --- | --- | --- |
| Page title | `24 / 32px` | `700` |
| Section title | `16 / 24px` | `650` |
| Card title | `14 / 20px` | `650` |
| Body | `14 / 22px` | `400` |
| Label | `13 / 20px` | `600` |
| Caption | `12 / 18px` | `400` |
| KPI | `24 / 30px` | `700` |

数字使用等宽数字特性。禁止使用负字距，不随视口缩放字号。

## Layout

- 桌面侧栏固定宽度 `216px`，使用白色背景和右侧细边框。
- 主内容最大宽度 `1600px`，在可用空间内居中。
- 页面头部由标题区和动作区组成；动作区只放当前页面的主要操作。
- Dashboard 采用 12 列栅格。卡片高度由内容决定，不通过同排等高制造空白。
- 同一分析主题优先使用标签页或分段视图，不一次堆叠所有图表。
- 页面区域不悬浮成装饰卡片；卡片只用于独立数据单元、重复条目和工具面板。

## Components

### Button

- 高度 `36px`，手机最小触控高度 `44px`
- 主按钮使用品牌蓝底白字
- 次按钮使用白底灰边
- 危险按钮使用危险红；页面中最多出现一个明显危险操作
- 图标在左、文字在右，图标统一 `16-18px`

### Card

- 白色背景、`1px` 灰色边框、`8px` 圆角
- 标题区与内容区使用间距或分割线组织，不使用彩色标题底
- Hover 只调整边框色和极轻的阴影，不改变布局

### Input

- 高度 `36px`，边框 `#D0D5DD`
- Label 永远位于控件上方
- Focus 使用品牌蓝边框和 3px 淡蓝焦点环
- Placeholder 使用弱化文本色，不代替 Label

### Table

- 表头使用 `#F9FAFB`
- 行高 `46px`，行间用细分割线
- 操作列靠右，常用操作使用图标按钮
- 手机端转为信息行卡片，但保持字段标签和值的对齐

### Badge And Tag

- Badge 只表达状态，使用语义色浅底
- Tag 表达分类，默认使用灰底；选中时使用淡蓝底
- 不为不同业务字段随意分配颜色

### Dialog And Drawer

- 标题、正文、操作区三段式
- 操作区右对齐，取消在左、主操作在右
- 遮罩使用 `rgba(16, 24, 40, 0.4)`

### Empty State

- 使用已有图标库中的线性图标
- 标题说明当前状态，正文给出下一步
- 仅在确有主操作时显示按钮

## Charts

- 系列色顺序：品牌蓝 `#2563EB`、绿色 `#12B76A`、橙色 `#F79009`、灰色 `#667085`
- 网格线使用 `#EAECF0`，坐标文字使用 `#667085`
- 图表标题、图例、Tooltip 和空状态使用统一排版
- Tooltip 为白底、灰边、8px 圆角，不使用深色气泡
- 不使用 3D、渐变填充、发光、过度动画
- 图表卡片必须同时给出数据口径或一句可行动洞察

## Responsive

- `> 1200px`：完整侧栏，多列 Dashboard
- `801-1200px`：完整侧栏，Dashboard 降为两列
- `<= 800px`：顶部栏 + 抽屉导航，所有主区域单列
- 手机端禁止横向页面溢出；复杂表格和矩阵可在自身容器内横向滚动
- 固定格式图表使用稳定的 `min-height` 或 `aspect-ratio`，避免数据加载时跳动

## System Page Patterns

### Metric Strip

- 记录、日期、工作当量、工时、项目、成果、目标等独立指标必须使用对应的线性图标，帮助快速识别。
- 图标放在 `32-42px` 的浅色语义容器中；图标颜色表达数据类别，不表达装饰。
- 同一行指标使用统一高度和数字基线；中等桌面与手机端自动重排，不设置导致横向溢出的最小总宽度。
- 指标卡只显示一个主要数值，不在卡内堆叠说明、按钮和图表。

### Analysis Workspace

- 报告首屏优先回答“投入去了哪里、什么占主导、能力结构如何”，趋势图和排行属于支持信息。
- 互补图表可在宽桌面并排；可用内容宽度不足时必须转为单列，不能压缩到文字截断或图形越界。
- 图表、排行和洞察高度由内容决定。禁止为了同排底部对齐强制拉高卡片。
- 自动洞察紧邻数据来源呈现，不单独占用大面积装饰卡片。

### Evidence Table

- 项目、成果、来源记录和材料完整度优先使用统一表格或分隔行，不为每一条记录创建独立卡片。
- 第一列负责对象名称和摘要，后续列依次呈现关联关系、投入/贡献、证据完整度、日期和状态。
- 桌面端保持列对齐；手机端转为有标签的信息行，表格自身可滚动，但页面不得横向滚动。

### Page Templates

- Dashboard：页面头部 → 指标条 → 核心结构分析 → 洞察 → 趋势/排行 → 明细。
- List：页面头部 → 指标条（可选）→ 单一筛选工具栏 → 数据质量（可选）→ 表格或分隔列表。
- Form：页面头部 → 按业务语义分段的表单 → 底部操作；不使用多层嵌套卡片。
- Detail：对象摘要 → 关键指标 → 关联成果/证据 → 时间线或原始明细。
- Report：周期与导出 → 指标条 → 结构洞察 → 代表性成果 → 成果包/证据 → 复盘与原始明细。

## Implementation Contract

### Source Of Truth

- 视觉决策以本文件为准，实际令牌只定义在 `frontend/src/styles/tokens.css`。
- `frontend/src/main.tsx` 是样式加载顺序的唯一入口，不得在页面中重复导入全局样式。
- 共享基础规则放入 `base.css`、`layout.css` 和 `components.css`。
- 页面域规则放入 `work-outcomes.css`、`growth-reports.css` 或 `settings-data.css`。
- `visual-refresh.css` 是当前统一视觉层，只处理跨页面设计语言和经过验证的页面组合，不保存临时试验。
- React 组件使用 `lucide-react` 线性图标；不得用 emoji、手写 SVG 或纯 CSS 图形替代功能图标。

### Change Workflow

1. 修改前确认页面属于 Dashboard、List、Form、Detail 或 Report 中的哪一种模板。
2. 优先复用现有组件和设计令牌，再新增局部类名；禁止直接写新的颜色、阴影、圆角和间距体系。
3. 同时检查有数据、无数据、长文本和窄屏状态，避免依靠固定高度填充页面。
4. 为可复发的布局或视觉约束补充 `frontend/test/styles.test.ts` 或 `uiFoundation.test.ts`。
5. 依次运行 `pnpm test`、`pnpm run typecheck`、`pnpm run build`。
6. 在 `1440x1024`、约 `829px` 宽度和 `390x844` 三个视口检查页面级溢出、文字遮挡、卡片异常留白和操作对齐。

### Reproduction Baseline

- 品牌与状态颜色、圆角、阴影、间距、内容宽度必须来自 `tokens.css`。
- 桌面端使用固定侧栏和紧凑多列信息布局；手机端使用顶部导航与单列主内容。
- KPI 采用“语义图标 + 标签 + 单一主数值”的稳定结构，图标与数字基线在同组卡片中保持一致。
- 分析模块在宽屏按信息关系并排，在空间不足时自然堆叠；禁止通过统一固定高度制造空白。
- 筛选工具栏在宽屏将主操作靠右，在窄屏按字段自然换行，页面本身不得横向滚动。
- 阶段性截图和 QA 产物只用于本地验证，不提交到仓库；最终规则必须回写本文件和测试。

## Review Checklist

每次 UI 修改完成后检查：

1. 是否只使用本文件定义的颜色、间距、圆角和字体层级？
2. 是否能删除一个不帮助理解或操作的视觉元素？
3. 标题、数据、说明和操作是否按重要性自然扫描？
4. 有数据与无数据时是否都保持紧凑、连续的页面节奏？
5. 桌面与手机端是否没有重叠、截断或页面级横向滚动？
6. 是否通过自动化测试、生产构建和浏览器视觉检查？
